export enum ValidationType {
  EMAIL = 'email',
  MAX = 'max',
  MAX_LENGTH = 'maxlength',
  MIN = 'min',
  MIN_LENGTH = 'minlength',
  PATTERN = 'pattern',
  REQUIRED = 'required',
  REQUIRED_TRUE = 'requiredTrue',
  VALIDATE_PATTERN = 'validatePattern' // Validação do KeyFilter do PrimeNG
}
